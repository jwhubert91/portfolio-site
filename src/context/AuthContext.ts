// Continue Firebase work at lesson 203, original set up at lesson 121
import { createContext, useReducer } from "react";

// @ts-ignore
export const AuthContext = createContext()

export const AuthContextProvider = ()=> {
  return null
}